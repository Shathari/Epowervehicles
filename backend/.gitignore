node_modules
dist
*.log

# Keep environment variables out of version control
.env

/src/generated/prisma

# Stray local database files (this app uses Postgres, but guard against accidental local artifacts)
*.db
*.db-journal

# User-uploaded product images (runtime data, not source)
/uploads
